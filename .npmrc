# Deliberately empty of dependency policy.
#
# Do NOT add `minimum-release-age` here: pnpm 11 silently ignores the kebab-case
# form in .npmrc (a 10-year cooldown set here still resolved the newest
# release). The supply-chain cooldown lives in pnpm-workspace.yaml as
# `minimumReleaseAge`.
engine-strict=true
